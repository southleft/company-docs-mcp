# Organization Configuration
ORGANIZATION_NAME="Your Company Name"
ORGANIZATION_DOMAIN="company.com"

# Branding (optional)
ORGANIZATION_LOGO_URL=""
ORGANIZATION_SUBTITLE="Powered by MCP (Model Context Protocol)"
ORGANIZATION_TAGLINE="Get instant answers from our comprehensive documentation."

# Supabase Configuration
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_KEY="your-service-role-key"

# Embedding Provider
# Auto-detected: if OPENAI_API_KEY is set, uses OpenAI. Otherwise uses Workers AI.
# Set explicitly to override: "workers-ai" or "openai"
# EMBEDDING_PROVIDER="workers-ai"

# Workers AI (default — no API key needed for deployed Worker)
# These are only required for the CLI ingestion script (npx company-docs publish)
CLOUDFLARE_ACCOUNT_ID="your-account-id"
CLOUDFLARE_API_TOKEN="your-api-token"

# OpenAI (optional — only needed if using OpenAI embeddings or AI chat)
OPENAI_API_KEY="sk-..."
OPENAI_MODEL="gpt-4o"

# Vector Search
VECTOR_SEARCH_ENABLED="true"
VECTOR_SEARCH_MODE="vector"

# Slack Integration (optional)
SLACK_SIGNING_SECRET="your-signing-secret"
SLACK_SLASH_COMMAND="/docs"
